import server from './src/app';

const PORT:number = 3001;

server.listen(PORT, () => {
    console.log("%s Listening",PORT, '\n')
})
// 9.30 midu Typescript node
//TODO format on type =>  Controla si el eitor debe dar ofrmato a la linea automaticamente despues de escribirla

// Cambiar fuente de vsCode

